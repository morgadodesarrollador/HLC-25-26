


jenkins:
  controller:
    JCasC:
      enabled: true
      defaultConfig: false # ⛔ importante: ya tienes tu propio ConfigMap
    
    configAsCode:
      enabled: false #Si NO haces esto, Helm intentará crear otro ConfigMap

    sidecars:
      configAutoReload:
        enabled: false
        # label: jenkins-jenkins-config # el configmap jenkins-jenkins-jcasc-config.yaml
        # folder: /var/jenkins_home/casc_configs
        # searchNamespace: jenkins
    # env:
    #   - name: CASC_JENKINS_CONFIG
    #     value: /var/jenkins_home/casc_configs

    image:
      repository: jenkins/jenkins
      tag: "2.541.1-lts-jdk21"

    runAsUser: 0
    fsGroup: 0

    admin:
      username: admin
      password: admin123

    persistence:
      enabled: true
      size: 8Gi
      storageClass: microk8s-hostpath   # 👈 clave

    serviceType: ClusterIP

    ingress:
      enabled: true
      ingressClassName: nginx
      hostName: jenkins.jamorgado.es
      # annotations:
      #   cert-manager.io/cluster-issuer: letsencrypt-prod
      #   nginx.ingress.kubernetes.io/ssl-redirect: "true"
      # tls:
      #   - secretName: jenkins-jamorgado-es-tls
      #     hosts:
      #       - jenkins.jamorgado.es
    installPlugins:
      - configuration-as-code
      - kubernetes
      - workflow-aggregator
      - credentials
      - github
      - git
  
  # 🔴 CLAVE: desactivar JCasC


  #     configScripts:
  #       base: |
  #         jenkins:
  #           systemMessage: "Jenkins desplegado con Helm + JCasC"
  #           numExecutors: 2

  #           securityRealm:
  #             local:
  #               allowsSignup: false
  #               users:
  #                 - id: "admin"
  #                   password: "admin123"

  #           authorizationStrategy:
  #             loggedInUsersCanDoAnything:
  #               allowAnonymousRead: false

  # 🔴 CLAVE: desactivar sidecar
  # sidecars:
  #   configAutoReload:
  #     enabled: false
  # sidecars:
  #   configAutoReload:
  #     env:
  #       - name: DISABLE_X509_STRICT_VERIFICATION
  #         value: "true"