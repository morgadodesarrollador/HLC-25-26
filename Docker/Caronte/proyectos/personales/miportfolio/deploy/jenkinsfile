pipeline {
  agent any

  stages {
    stage('Checkout2') {
      steps {
        echo "Repositorio3 clonado correctamente"
        sh 'ls -la'
      }
    }

    stage('Info2') {
      steps {
        sh 'git rev-parse --short HEAD'
      }
    }
  }
}
