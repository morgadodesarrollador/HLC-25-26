apiVersion: v1
kind: Service
metadata:
  name: service-miportfolio
  namespace: {{ .Release.Namespace }}
spec:
  type: NodePort
  selector:
    app: miportfolio
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30087      
